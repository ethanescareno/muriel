console.log(Contacts.find().count());
Meteor.methods({
  // insertContacts: function(contacts) {
    // contacts.forEach(function(cont) {
    //   delete cont._id;
    //   console.log("este es un contacto", cont);
    //   Contacts.insert(cont);
    // })
  // }
});
